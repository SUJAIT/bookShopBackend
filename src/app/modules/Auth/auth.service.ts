import { IUser, TNormalUserLogin } from "./auth.interface";

const loginUser = async (payload:TNormalUserLogin | IUser)=>{

    const user = await User.isUserExistsByCustomId

}

export const AuthServices = {
    loginUser
}